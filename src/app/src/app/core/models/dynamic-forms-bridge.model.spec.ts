import { DynamicFormsBridge } from './dynamic-forms-bridge.model';

describe('DynamicFormsBridge', () => {
  it('should create an instance', () => {
    expect(new DynamicFormsBridge()).toBeTruthy();
  });
});

import { EmploymentStatus } from './employment-status.model';

describe('EmploymentStatus', () => {
  it('should create an instance', () => {
    expect(new EmploymentStatus()).toBeTruthy();
  });
});

import { InputControlTimeOnlyTo } from './input-control-time-only-to.control';

describe('InputControlTimeOnlyTo', () => {
  it('should create an instance', () => {
    expect(new InputControlTimeOnlyTo()).toBeTruthy();
  });
});

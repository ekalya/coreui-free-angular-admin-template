import { InputControlDropdown } from './input-control-dropdown.control';

describe('InputControlDropdown', () => {
  it('should create an instance', () => {
    expect(new InputControlDropdown()).toBeTruthy();
  });
});
